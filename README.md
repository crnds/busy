# busy

BUSY Bar display clone for the Cheap Yellow Display (ESP32-2432S028R).

Firmware lives in [`cyd-busybar/`](cyd-busybar/). Plan: [`brief.md`](brief.md).

```bash
cd cyd-busybar
pio run
pio run -t upload && pio run -t uploadfs
```
